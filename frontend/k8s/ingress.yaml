# Kubernetes Ingress for esprit-market-frontend
# Generated by Containerization Assist
# Note: This requires an Ingress Controller to be installed in your kubernetes cluster
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: esprit-market-frontend
  namespace: default
  labels:
    app: esprit-market-frontend
  annotations:
    # NGINX Ingress Controller
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    # TLS Certificate (configure with cert-manager)
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  # Uncomment and configure TLS if using HTTPS
  # tls:
  #   - hosts:
  #       - esprit-market.example.com
  #     secretName: esprit-market-tls
  
  rules:
    - host: esprit-market.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: esprit-market-frontend
                port:
                  number: 80
